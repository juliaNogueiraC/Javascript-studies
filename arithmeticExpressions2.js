/*
combination of operands, operatons  that can be evaluted to a value  

*/

let student = 20;
student = student + 1;
// student = student * 1;
// student = student / 1;
//student +=1;
let extraStudents = student % 3;
console.log(student);
console.log(extraStudents);